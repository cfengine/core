***********************************************************
 Promises 
***********************************************************
Bundle databases in the context of agent

   ARGS:

   {
   TYPE: databases

      databases promise by 'cf_topic_maps/topics' (implicit) if context is any

         database_operation => create       if body context any
         database_type => sql       if body context any
         database_columns =>  {'topic_name,varchar,256','topic_comment,varchar,1024','topic_id,varchar,256','topic_type,varchar,256','topic_extra,varchar,26'}       if body context any
         database_server => myserver body for type database_server(no parameters)
               {
               db_server_owner => postgres if sub-body context none
               db_server_password =>  if sub-body context none
               db_server_host => localhost if sub-body context none
               db_server_type => postgres if sub-body context none
               db_server_connection_db =>  if sub-body context none
               db_server_owner => root if sub-body context any
               db_server_password =>  if sub-body context any
               db_server_host => localhost if sub-body context any
               db_server_type => mysql if sub-body context any
               db_server_connection_db => mysql if sub-body context any
               }
       if body context any
            Promise (version not specified) belongs to bundle 'databases' (type agent) in file '../tests/units/unit_sql.cf' near line 20

   }




All Bodies

control body for type common(no parameters)
   {
   bundlesequence =>  {'databases'} if sub-body context any
   }

myserver body for type database_server(no parameters)
   {
   db_server_owner => postgres if sub-body context none
   db_server_password =>  if sub-body context none
   db_server_host => localhost if sub-body context none
   db_server_type => postgres if sub-body context none
   db_server_connection_db =>  if sub-body context none
   db_server_owner => root if sub-body context any
   db_server_password =>  if sub-body context any
   db_server_host => localhost if sub-body context any
   db_server_type => mysql if sub-body context any
   db_server_connection_db => mysql if sub-body context any
   }

as_user body for type contain
   arg x

   {
   exec_owner => $(x) if sub-body context any
   }

