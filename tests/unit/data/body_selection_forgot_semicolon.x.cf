body classes surfsara_all_classes(x)
{
      promise_repaired => { "$(x)_repaired" };
      promise_kept     => { "$(x)_kept"     }
      repair_failed    => { "$(x)_failed"   };
      repair_denied    => { "$(x)_denied"   };
      repair_timeout   => { "$(x)_timeout"  };
}
