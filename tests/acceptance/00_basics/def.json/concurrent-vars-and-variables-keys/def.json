{
    "vars": {
        "my_var": "Set by vars key",
        "in_vars": "Only in vars"
    },
    "variables": {
        "default:def.my_var": "Set by variables key",
        "in_variables": "Only in variables"
    }
}
