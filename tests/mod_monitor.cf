#########################################################

body monitor control()
   {
   threshold => "0.3"; 

   forgetrate => "0.7";

   histograms => "true";

   tcpdump => "false";

   # on linux

   linux::

   # sensor => readfile("/proc/cpu/temperature");
   }

