language: android
dist: trusty
android:
  components:
    - build-tools-29.0.2
    - android-29
    - add-on
    - extra
  licenses:
    - 'android-sdk-license-.+'
    - 'google-gdk-license-.+'

before_script:
  # debug what build-tools are available
  - sudo find / -name 'sdkmanager' || true
  - command -v sdkmanager || true
  - sudo find / -name 'avdmanager' || true
  - sudo find / -name 'emulator' || true
  - sudo find / -name 'android' || true
  - command -v avdmanager || true
  - command -v emulator || true
  - command -v android || true
  - sdkmanager --list
#  - find / -name 'avdmanager'
#  - find / -name 'android'
#  - which android
#  - android --help
#  - android --verbose list
  
#  - echo no | android create avd --force -n test -t android-26 --abi armeabi-v7a -c 100M
#  - emulator -avd test -no-audio -no-window &
#  - android-wait-for-emulator
#  - adb shell input keyevent 82 &
